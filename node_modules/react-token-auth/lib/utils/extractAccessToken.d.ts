import { Maybe, TokenString } from '../types';
export declare const extractAccessToken: <Session>(session: Maybe<Session>, getAccessToken?: ((session: Session) => TokenString) | undefined) => Maybe<string>;
