import { Maybe } from './types';
export declare const createTokenUpdater: <Session>(onUpdateToken: (session: Session) => Promise<Maybe<Session>>) => {
    updateToken: (session: Session) => Promise<Maybe<Session>>;
};
