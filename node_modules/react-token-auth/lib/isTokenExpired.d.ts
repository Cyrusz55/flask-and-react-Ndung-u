import { SimpleLogger } from './logger';
import { TokenString } from './types';
export declare const isTokenExpired: (token: TokenString, thresholdMillisec?: number | undefined, logger?: SimpleLogger | undefined) => boolean;
