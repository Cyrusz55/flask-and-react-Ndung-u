import { IListenerContainer } from './createListenersContainer';
import { Getter, Maybe } from './types';
export declare const createUseAuth: <Session>({ getSessionState, onHydratation, listenersContainer, }: {
    getSessionState: Getter<Maybe<Session>>;
    listenersContainer: IListenerContainer;
    onHydratation?: ((session: Maybe<Session>) => void) | undefined;
}) => () => [boolean, Maybe<Session>];
